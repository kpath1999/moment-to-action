Renames marked with ->

If a folder has only files starting with _, export everything in __init__.py

Create a BaseMessage with just timestamp that inherits from pydantic.BaseModel

Backends should not fallback to CPU internally, they should error and force ComputeBackend to fallback

Move everything in edgeperceieve/pipeline to scripts/ in repo root and update to use new code

src/moment_to_action
	- messages/
		- __init__.py 
		- _base.py
		- sensor.py (RawFrameMessage)
		- audio.py (AudioTensorMessage)
		- video.py (TensorMessage -> FrameTensorMessge, BoundingBox, DetectionMessage)
		- llm.py (ReasoningMessage)
		- vlm.py (ClassificationMessage)
	- hardware/
		- __init__.py
		- _types.py (ComputeUnit - get rid of "QCS4940" thing, PowerSample)
		- _backend.py (ComputeBackend, should auto-select based on platform)
		- _platforms/
			- __init__.py
			- _base.py (ABCs for PowerMonitor, InferenceBackend)
			- qcs6940/
				- __init__.py
				- _power.py (PowerMonitor)
				- _cpu.py (CPUBackend)
				- ... other backends
	- metrics/
		- copy as-is
	- sensors/
		- base sensor
		- "file" sensor (just loads from file)
	- stages/
		- audio/
			- __init__.py (placeholder)
		- video/
			- video preprocessors and models
		- llm/
			- llm preprocessors and models
		- vlm/
			- vlm preprocessors and models
